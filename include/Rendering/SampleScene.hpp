#pragma once
#include "Scene.hpp"
/*

class SampleScene{
public:
  Buffer cube_buffer;
  Scene scene;
  std::shared_ptr<NodeBase> root;
SampleScene(){
  root = std::make_shared<NodeBase>();
  root->setPos( 1.0f,1.0f,1.0f);
  std::shared_ptr<ShapeNode> snode  = std::make_shared<ShapeNode>(cube_buffer);
  snode->setPos( -1.0f,0.0f,0.0f );
  std::shared_ptr<ShapeNode> snode2 = std::make_shared<ShapeNode>(cube_buffer);
  snode2->setPos(1.0f,0.0f,0.0f);
  root->addChild(snode);
  root->addChild(snode2);
  scene->setRoot(root);


}
void Draw(){
  
}

}



*/